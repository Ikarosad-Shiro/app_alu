<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.aluasistencias">


    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AluAsistencias"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
<<<<<<< HEAD
            android:value="@string/google_maps_api_key" />

        <activity
            android:name=".VistaMapa"
            android:exported="false" />
        <activity
            android:name=".Menujava"
=======
            android:value="AIzaSyDQdshjnmu9zDNLTeAJOUQ_TfkI4U1t9OY" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Registra la actividad AddRecordActivity -->
        <activity
            android:name=".AddRecordActivity"
>>>>>>> origin/fernando
            android:exported="false" />

        <!-- Registra la actividad Menujava -->
        <activity
            android:name=".Menujava"
            android:exported="false" />

        <!-- Registra la actividad RegisterActivity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Registra la actividad EditRecordActivity -->
        <activity
            android:name=".EditRecordActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
